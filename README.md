# Navegar até a pasta do servidor
cd back-react-shop/server

# Instalar as dependências
npm install

# Iniciar o servidor em modo de desenvolvimento
npm run dev

# Navegar até a pasta do frontend
cd front-react-shop

# Instalar as dependências
npm install

# Iniciar a aplicação
npm run dev

